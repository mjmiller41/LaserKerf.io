/**
 * golden — the golden-output test harness (development-plan §4.3).
 * Design + settings -> machine code must byte-match a committed fixture.
 */
export { assertGolden, type GoldenOptions } from './golden';
