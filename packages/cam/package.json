{
  "name": "cam",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CAM core: cut settings, layer cut modes (line/fill/offset-fill/fill+line) -> toolpaths, cut-order optimization, material library, and test-grid generation. Pure TS; consumes the scene model and geometry-wasm.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:golden": "vitest run golden"
  },
  "dependencies": {
    "geometry-wasm": "workspace:*",
    "scene": "workspace:*"
  },
  "devDependencies": {
    "golden": "workspace:*"
  }
}
