{
  "name": "device-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Transport-agnostic Device abstraction + transport registry + FakeDevice simulator. UI/CAM code depends ONLY on this — never on a concrete transport (CLAUDE.md invariant 2).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
