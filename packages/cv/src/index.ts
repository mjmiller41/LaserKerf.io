/**
 * cv — OpenCV.js (WASM) camera-calibration wrappers + AprilTag helpers.
 * Scaffold for M0; populated in M5 (camera capture, lens calibration, homography).
 */
export const CV_SCAFFOLD = true as const;
