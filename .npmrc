# Keep peer resolution forgiving across the fast-moving 2026 toolchain majors.
auto-install-peers=true
strict-peer-dependencies=false
# Deterministic engine behaviour without hard-failing on minor Node drift.
engine-strict=false
