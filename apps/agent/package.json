{
  "name": "agent",
  "version": "0.0.0",
  "private": true,
  "description": "Fluence native companion Agent (Rust). Wraps cargo so Turborepo/pnpm can orchestrate it alongside the JS workspace.",
  "scripts": {
    "build": "cargo build",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "typecheck": "cargo check --all-targets",
    "format": "cargo fmt",
    "verify": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo build && cargo test"
  }
}
